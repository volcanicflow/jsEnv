function funStuff() {
  //set up something else after initial page load
  var specializedState = {};
  console.log("still got something for you, hang on, just a sec");

  return {
    specializedState: "spec"
  };
}

export default funStuff;
